import { ClassButton } from "../../_components/classButton";

export const ShowMoreBuildButton = () => {
  return (
    <ClassButton href="/morebuild" className="w-full sm:w-60">
      show more build
    </ClassButton>
  );
};