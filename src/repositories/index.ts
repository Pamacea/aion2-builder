/**
 * Repositories barrel export
 *
 * Provides clean imports for all repository classes
 *
 * @example
 * ```ts
 * import { buildRepository } from '@/repositories';
 * ```
 */

export { BuildRepository, buildRepository } from './build.repository';
